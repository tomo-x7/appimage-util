
# appimage-util 仕様（詳細）

## 1. 目的

AppImage と、その起動に必要なデスクトップエントリ（`.desktop`）の管理を、CachyOS + KDE 環境で簡単に行う対話型シェルユーティリティを提供する。

本ツールが担うこと：

- `~/.appimages/{appname}/` をアプリ単位の管理ディレクトリとして作成・更新・削除
- その配下に AppImage 本体、`.desktop` 原本、アイコンを配置
- `.desktop` の配布先として `~/.local/share/applications/generated.{appname}.desktop` を生成（原本のコピー）

本ツールが担わないこと：

- MIME type 関連付け（`MimeType=`）
- 隠しエントリ（`NoDisplay=true`）
- `Exec=` プレースホルダ（`%f` 等）
- `Path=` の指定

## 2. 想定環境

- OS: CachyOS（Arch 系）
- DE: KDE Plasma
- Node.js: `>= 22`（SEA で単一実行バイナリ化する想定）

## 3. 用語

- **appname**: アプリ識別子。ディレクトリ名・ファイル名の基礎。ユーザー表示名（`Name=`）とは別。
- **原本 desktop**: `~/.appimages/{appname}/{appname}.desktop`。常に正。
- **生成 desktop**: `~/.local/share/applications/generated.{appname}.desktop`。原本のコピー。

## 4. ディレクトリ/ファイル構成

### 4.1 ルート

- AppImage ルート: `~/.appimages/`
- desktop 配置先: `~/.local/share/applications/`

### 4.2 アプリディレクトリ

`~/.appimages/{appname}/` の中身：

- `{appname}.appimage`（必須）
- `{appname}.desktop`（必須、原本）
- `icon.png` / `icon.svg` など（任意、後述のルールで命名）

### 4.3 生成 desktop

- `~/.local/share/applications/generated.{appname}.desktop`

生成 desktop は原本 desktop の内容をそのままコピーする（必要に応じてファイル名だけ変える）。

## 5. デスクトップエントリ仕様（本ツールが生成/編集する範囲）

グループは `[Desktop Entry]` のみを扱う。

### 5.1 固定値

- `Version=1.2`
- `Type=Application`
- `Terminal=false`

### 5.2 対話で取得する値

- `Name=`（必須）: ランチャー表示名（日本語名のみでよい。英語名の入力は必須にしない）
- `Comment=`（任意）: 簡単な説明
- `Categories=`（必須・1つ）: 末尾 `;` を付けて保存
- `Keywords=`（任意）: `foo;bar;` 形式に正規化して保存
- `Icon=`（任意）: **絶対パス**を基本とする（例: `Icon=/home/USER/.appimages/foo/icon.png`）

### 5.3 自動決定する値

- `Exec=`: `~/.appimages/{appname}/{appname}.appimage`
	- プレースホルダは使わない
	- 引数追加も行わない（必要なら今後の拡張点）

### 5.4 出力例

```ini
[Desktop Entry]
Version=1.2
Type=Application
Name=Example App
Comment=Example AppImage
Exec=/home/USER/.appimages/example/example.appimage
Icon=/home/USER/.appimages/example/icon.png
Categories=Utility;
Keywords=example;tool;
Terminal=false
```

## 6. アプリ一覧の定義

- `~/.appimages` 直下の **ディレクトリ名一覧**をアプリ一覧とする
- 各 `{appname}` ディレクトリには `{appname}.desktop` が存在することを前提
	- 存在しない場合は「壊れたエントリ」として扱い、選択 UI では末尾に `(broken)` を付けて表示する
	- 壊れたエントリは `update` / `edit` を拒否し、`uninstall` は許可する

## 7. CLI 仕様

### 7.1 共通

- サブコマンド未指定時：サブコマンド一覧を表示して終了（exit code 0）
- パスは相対パス可（内部で `process.cwd()` 基準で解決）
- appname の検証（インストール時）：
	- 禁止: 空、`/`、空白、制御文字、全角文字など「ファイル名に使うべきでない文字」
	- 仕様としては次の正規表現に合致するもののみ許可：
		- `^[a-z0-9][a-z0-9._-]{0,62}$`（小文字英数、`.`, `_`, `-`）
	- 既存 appname と重複は不可

### 7.2 install

目的：新規 AppImage を管理下に導入し、desktop を生成する。

呼び出し：

- `appimage-util install <path-to-appimage> [appname]`
	- 第1引数（必須）: AppImage へのパス
	- 第2引数（任意）: appname（省略時は質問）

対話で行うこと：desktop に必要な情報を質問で受け取る（カテゴリ等は select）。

実操作は **全質問完了後** にまとめて行う。

実操作手順（成功時のみ commit する考え方）：

1. `~/.appimages/{appname}/` を作成
2. AppImage と選択されたアイコンを **リネームしつつコピー**
	 - AppImage: `{appname}.appimage`
	 - Icon: `icon.<ext>`（`png` / `svg` を想定）
	 - `{appname}.appimage` には必ず実行権を付与する（`chmod 755` 相当）
3. `~/.appimages/{appname}/{appname}.desktop` を作成
4. `~/.local/share/applications/generated.{appname}.desktop` にコピー
5. 元の AppImage（第1引数で渡されたファイル）を削除
	- 削除前に確認を挟む：`元の AppImage を削除します [Y/n]`

### 7.3 update

目的：追加済みパッケージを選択し、渡された AppImage で更新する。

呼び出し：

- `appimage-util update <path-to-new-appimage> [--force]`

動作：

- 対話で更新対象 appname を選択
- 新旧 AppImage の SHA-256 を計算し比較
	- 同一なら `--force` が無い限り更新しない
	- `--force` 時は警告を出して続行可能
- 実行前に確認を挟む：`{appname} を {newAppImageFileName} で上書きします [y/N]`

更新手順：

1. 既存 `{appname}.appimage` を `{appname}.old.appimage` にリネーム
	- 既に `{appname}.old.appimage` が存在する場合は削除して 1 世代のみ保持する
2. 新しい AppImage を `{appname}.appimage` にリネームしつつ移動（またはコピー→元削除）
	- `{appname}.appimage` には必ず実行権を付与する（`chmod 755` 相当）
3. desktop は原本を変更しない（`Exec` は固定パスのため通常不要）

バックアップは常に「直前 1 世代のみ」。

### 7.4 edit

目的：desktop 原本を編集し、生成 desktop を更新する。

呼び出し：

- `appimage-util edit`

動作：

- 対話で appname を選択
- install と同等の質問を行う
	- **デフォルト値は既存 desktop の値**
	- appname は変更不可
- アイコンは以下 4 択から選択（要件）：
	- そのまま（現状維持）
	- 手動指定（ファイルパス入力 → コピー）
	- 抽出（管理下の `{appname}.appimage` から抽出して選択）
	- なし（`Icon` キー削除 + 既存 `icon.*` も削除）

適用手順：

1. `~/.appimages/{appname}/{appname}.desktop` を更新
2. `~/.local/share/applications/generated.{appname}.desktop` にコピー

### 7.5 uninstall

目的：追加済みパッケージを削除する。

呼び出し：

- `appimage-util uninstall`

動作：

- 対話で appname を選択
- 確認：`{appname} を削除します [y/N]`
- 実行：
	- `~/.local/share/applications/generated.{appname}.desktop` を削除
	- `~/.appimages/{appname}/` を削除（再帰）

## 8. 対話 UI 仕様（質問内容）

### 8.1 install/edit 共通質問

- 表示名（`Name`）: input（必須、日本語名のみでよい）
- 説明（`Comment`）: input（任意、空で省略）
- カテゴリ（`Categories`）: select（必須、単一）
- キーワード（`Keywords`）: input（任意）
	- 入力は `,` または `;` 区切りを許可
	- 保存時は `foo;bar;` に正規化（末尾 `;` 必須）
	- 区切り前後の空白はトリムし、キーワード内部の空白は保持する
	- 連続区切り・空要素は除去する（例：`,,` や `foo;;bar` は無視）
	- キーワード内の空白は許可する（例：`image editor`）

### 8.2 カテゴリ選択肢（単一）

最小で以下を提供する（必要なら追加）：

- `AudioVideo`
- `Development`
- `Education`
- `Game`
- `Graphics`
- `Network`
- `Office`
- `Science`
- `Settings`
- `System`
- `Utility`

保存値は `Categories=<選択値>;`。

## 9. Icon 仕様（抽出/手動/なし）

### 9.1 抽出の基本方針

- install: AppImage から「有力な順に最大 10 個」抽出して選択可能にする
	- 有力な場所に存在しない場合：
		- 「アイコン無しで作成」または「手動で選択」を選べる
- edit: 「そのまま / 手動指定 / 抽出 / なし」の 4 択

### 9.2 抽出方法（tmp 展開）

AppImage を tmp ディレクトリに展開して探索する。

- 展開手段：`<appimage> --appimage-extract`
	- 実行するために exec 権限が必要なため、入力 AppImage を tmp にコピーして `chmod +x` した上で展開する（入力ファイルを直接変更しない）
	- 展開先は tmp 内の `squashfs-root/` を想定

### 9.3 探索対象と優先度（スコアリング）

候補は `png` / `svg` を対象とし、以下を優先する。

優先度の概念（例）：

1. `squashfs-root/.DirIcon`（存在する場合）
2. `squashfs-root/usr/share/icons/hicolor/**/apps/*.{png,svg}`
3. `squashfs-root/usr/share/pixmaps/*.{png,svg}`
4. `squashfs-root/**/icons/**/apps/*.{png,svg}`
5. 上記以外の `*icon*` を含むパス

同一スコアなら次で比較：

- 画像サイズが大きい（PNG の場合）
- 互換性を優先し、基本は PNG を SVG より優先する（SVG は環境によって扱いが揺れるため）

※PNGの解像度取得は、可能ならファイルのメタから取得して UI 表示に含める（ライブラリ選定は後述）。

補足：`.DirIcon` は拡張子が無い場合があるため、実体が `png`/`svg` の場合のみ候補に含める。

### 9.4 選択 UI

- 抽出した候補を最大 10 件表示
	- 表示例：`icon (256x256) - usr/share/icons/hicolor/256x256/apps/foo.png`
- プレビュー表示を可能にする
	- プレビューとは、該当ファイルを「その拡張子の画像を表示する既定アプリ」で開くこと
	- 実装は `xdg-open <iconFilePath>` を用いる（KDE/Arch 系で一般的）
	- CLI の選択 UI としては、少なくとも以下のいずれかの UX を満たせばよい：
		- (A) 「候補を選ぶ」前に「プレビューする」を選べ、候補を指定してプレビューできる（プレビュー後は候補選択へ戻る）
		- (B) 候補を一度選択したあとに「このアイコンをプレビューしますか？」の確認を挟み、プレビュー後に採用/再選択を選べる
	- `xdg-open` の起動は可能なら非同期（待たずに選択 UI を継続）とし、失敗時は警告を表示して続行する
- 選ばれた 1 件をアプリディレクトリに `icon.<ext>` としてコピー
- `Icon=` にはその絶対パスを記録

### 9.5 手動指定

- ユーザーが画像ファイルパスを入力
- `png` / `svg` のみ許可（その他はエラー）
- 入力後、必要ならプレビュー（`xdg-open`）できる導線を用意する（任意）
- アプリディレクトリに `icon.<ext>` としてコピーして `Icon=` に反映

### 9.6 なし

- `Icon=` キーを出力しない（または削除）

## 10. ファイル操作/原子性（失敗時の扱い）

### 10.1 install

- 質問フェーズではファイル操作を行わない
- 実操作は tmp ステージング（例：`~/.appimages/.staging.{appname}.{pid}`）で生成し、最後に `rename` で確定する方針
	- ただし最終的な要件は「質問後に実際の操作を行う」なので、内部実装の都合で staging は許容
- 元 AppImage の削除は、全手順成功後の最後に行う

### 10.2 update

- 既存を `.old.appimage` にリネームしてから置換する
- 置換に失敗した場合：復旧は自動で行わず、そのままエラー終了してよい
  - `.old.appimage` は「新 AppImage に不具合があった時の退避用途」とする

## 11. 実装方針（TypeScript / Node ESM）

- エントリポイント: `src/index.ts`
- CLI: サブコマンドベース（install/update/edit/uninstall）
- 対話 UI: select/input/confirm
- `.desktop` は INI ライクだが、最低限 `[Desktop Entry]` のキー・バリューのみ読み書きできればよい

`.desktop` の出力順序は以下に固定する（差分を安定させるため）：

1. `Version`
2. `Type`
3. `Name`
4. `Comment`（存在する場合のみ）
5. `Exec`
6. `Icon`（存在する場合のみ）
7. `Categories`
8. `Keywords`（存在する場合のみ）
9. `Terminal`

## 12. 依存ライブラリ（npm）案

要件：著名（目安：週 DL 1 万程度以上）。導入時は `pnpm add` を用い、`package.json` を手編集しない。

採用候補（確定案）：

- CLI 解析：`commander`
- 対話プロンプト：`@inquirer/prompts`
- 子プロセス実行：`execa`
- ファイル探索（アイコン候補収集）：`fast-glob`
- INI パース/生成：`ini`
- PNG サイズ取得（候補表示のため、任意）：`image-size`

インストール例：

```bash
pnpm add commander @inquirer/prompts execa fast-glob ini image-size
```

（`image-size` は UI 表示を省略するなら不要）

